name: Facebook zero-nicotine gallery repair

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: facebook-zero-nicotine-gallery-repair
  cancel-in-progress: false

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate zero-nicotine Facebook logic
        run: |
          node --check tools/facebook-publisher.js
          node tools/check-facebook-publisher.js
          node tools/facebook-publisher.js --check-repair-zero-nicotine-galleries

      - name: Repair existing Facebook galleries
        id: repair
        continue-on-error: true
        env:
          FACEBOOK_PAGE_ID: ${{ secrets.FACEBOOK_PAGE_ID }}
          FACEBOOK_PAGE_ACCESS_TOKEN: ${{ secrets.FACEBOOK_PAGE_ACCESS_TOKEN }}
        run: node tools/facebook-publisher.js --repair-zero-nicotine-galleries

      - name: Record every confirmed Facebook change
        if: always()
        shell: bash
        run: |
          if git diff --quiet -- data/facebook-publish-state.json data/facebook-campaign-state.json; then
            echo "No Facebook state change to record."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/facebook-publish-state.json data/facebook-campaign-state.json
          git commit -m "Record zero-nicotine Facebook galleries"
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Report an incomplete repair
        if: steps.repair.outcome == 'failure'
        run: |
          echo "At least one Facebook change failed. Every confirmed earlier change was preserved in state."
          exit 1
