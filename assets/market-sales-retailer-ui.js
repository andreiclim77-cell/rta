(function(){
  'use strict';
  var ID='marketSalesByRetailer',timer=null;
  function byId(i){return document.getElementById(i)}
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
  function en(){return window.__rtaLang==='en'}function t(r,e){return en()?e:r}
  function build(sales,market){var root=byId('market2026Root');if(!root||!root.querySelector('.market-hero'))return;var old=byId(ID);if(old)old.remove();var names={};(market.retailers||[]).forEach(function(r){names[r.id]=r});var ranks=sales.rankings||[],actual=sales.actualSales||[],statuses=sales.retailerStatus||[],ids=(market.retailers||[]).map(function(r){return r.id});var box=document.createElement('section');box.id=ID;box.className='market-report-section';var rows=ids.map(function(id){var rr=ranks.filter(function(x){return x.retailerId===id}).sort(function(a,b){return Number(a.rank)-Number(b.rank)}),aa=actual.filter(function(x){return x.retailerId===id}),st=statuses.filter(function(x){return x.retailerId===id}),tier=aa.length?'A':rr.length?'B':'—',methods=Array.from(new Set(st.map(function(x){return x.method}).filter(Boolean))),sources=Array.from(new Set(st.map(function(x){return x.source}).filter(Boolean))),errors=st.filter(function(x){return x.error}).map(function(x){return x.error});return{id:id,name:names[id]&&names[id].name||id,url:names[id]&&names[id].url||'',tier:tier,rankings:rr.length,actual:aa.length,methods:methods,sources:sources,errors:errors,top:rr.slice(0,3)}});box.innerHTML='<h3>'+esc(t('Sinteză comercială pe fiecare magazin','Commercial summary for every store'))+'</h3><div class="market-report-note">'+esc(t('Tier A = unități vândute explicit verificabile. Tier B = bestseller/popularitate publicată de magazin. „—” = nu am găsit încă un semnal de vânzare public verificabil; nu îl înlocuim cu stoc sau presupuneri.','Tier A = explicitly verifiable units sold. Tier B = retailer-published bestseller/popularity. “—” = no public verifiable sales signal found yet; we do not replace it with stock or assumptions.'))+'</div><div class="market-report-table-wrap"><table class="market-report-table"><thead><tr><th>'+esc(t('Magazin','Store'))+'</th><th>Tier</th><th>'+esc(t('Semnale','Signals'))+'</th><th>'+esc(t('Top observat','Observed top'))+'</th><th>'+esc(t('Metodă','Method'))+'</th><th>'+esc(t('Stare','State'))+'</th></tr></thead><tbody>'+rows.map(function(r){var top=r.top.map(function(x){return'#'+x.rank+' '+x.product}).join(' · '),state=r.tier==='—'?t('în căutare','searching'):r.errors.length?t('date + erori de sursă','data + source errors'):t('verificat','verified');return'<tr><td><a target="_blank" rel="noreferrer" href="'+esc(r.url)+'"><strong>'+esc(r.name)+'</strong></a></td><td><strong>'+esc(r.tier)+'</strong></td><td>'+esc(t('ranking','ranking'))+': '+r.rankings+' · '+esc(t('contoare','counters'))+': '+r.actual+'</td><td>'+esc(top||'—')+'</td><td>'+esc(r.methods.join(', ')||'—')+'</td><td>'+esc(state)+(r.errors.length?' · '+esc(r.errors.slice(0,2).join('; ')):'')+'</td></tr>'}).join('')+'</tbody></table></div>';var anchor=byId('marketSalesIntelligence')||byId('market2026FullReport');if(anchor&&anchor.parentNode)anchor.parentNode.insertBefore(box,anchor.nextSibling);else root.appendChild(box)}
  function load(){return Promise.all([fetch('/data/market-sales-2026.json?live='+Date.now(),{cache:'no-store'}).then(function(r){if(!r.ok)throw 0;return r.json()}),fetch('/data/market-2026.json?live='+Date.now(),{cache:'no-store'}).then(function(r){if(!r.ok)throw 0;return r.json()})]).then(function(x){build(x[0],x[1])}).catch(function(){})}
  function schedule(){clearTimeout(timer);timer=setTimeout(load,150)}function boot(){new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true});schedule()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()
})();
