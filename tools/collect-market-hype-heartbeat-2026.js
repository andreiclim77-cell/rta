#!/usr/bin/env node
'use strict';
const fs=require('fs'),crypto=require('crypto');
const CFG='data/market-hype-sources-2026.json',OUT='data/market-hype-heartbeat-2026.json',EVID='data/market-hype-heartbeat-evidence-2026.json',WRITE=process.argv.includes('--write');
function read(p,f){try{return JSON.parse(fs.readFileSync(p,'utf8'))}catch(_){return f}}
function stable(v){return JSON.stringify(v,null,2)+'\n'}
function writeIfChanged(p,v){const next=stable(v),prev=fs.existsSync(p)?fs.readFileSync(p,'utf8'):'';if(next===prev)return false;fs.writeFileSync(p,next,'utf8');return true}
function norm(v){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim()}
function clean(v){return String(v||'').replace(/<!\[CDATA\[|\]\]>/g,'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;|&apos;/g,"'").replace(/<script\b[\s\S]*?<\/script>/gi,' ').replace(/<style\b[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim()}
function hash(v){return crypto.createHash('sha256').update(String(v||'')).digest('hex').slice(0,20)}
function host(u){try{return new URL(String(u||'')).hostname.replace(/^www\./,'').toLowerCase()}catch(_){return''}}
function domainMatch(h,d){return Boolean(h&&d&&(h===d||h.endsWith('.'+d)))}
function iso(v){const ms=Date.parse(String(v||''));return Number.isFinite(ms)?new Date(ms).toISOString():null}
function ageHours(v){const ms=Date.parse(String(v||''));return Number.isFinite(ms)?Math.max(0,(Date.now()-ms)/36e5):null}
function unique(a){return[...new Set((a||[]).filter(Boolean))]}
function tag(block,name){const m=String(block||'').match(new RegExp(`<${name}[^>]*>([\\s\\S]*?)<\\/${name}>`,'i'));return m?clean(m[1]):''}
async function fetchText(url,timeout=9000){const c=new AbortController(),tm=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{redirect:'follow',headers:{'user-agent':'Ghid-RTA-Hype-Heartbeat/1.0 (+https://ghid-rta.ro/)','accept':'application/rss+xml,application/xml,text/html;q=0.9,*/*;q=0.5','accept-language':'en,ro;q=0.8'},signal:c.signal});const text=await r.text();if(!r.ok)throw new Error(`HTTP ${r.status}`);return{url:r.url||url,text}}finally{clearTimeout(tm)}}
function rss(xml,meta){const out=[];for(const m of String(xml||'').matchAll(/<item>([\s\S]*?)<\/item>/gi)){const b=m[1],title=tag(b,'title'),body=tag(b,'description'),url=tag(b,'link'),publishedAt=iso(tag(b,'pubDate')||tag(b,'dc:date'));if(title||body)out.push({...meta,title,body,url,publishedAt})}return out}
async function bing(meta){try{const f=await fetchText('https://www.bing.com/search?format=rss&q='+encodeURIComponent(meta.query));return{ok:true,meta,docs:rss(f.text,meta)}}catch(e){return{ok:false,meta,docs:[],error:String(e&&e.message||e).slice(0,120)}}}
async function mapLimit(items,limit,fn){const out=new Array(items.length);let i=0;async function worker(){for(;;){const n=i++;if(n>=items.length)return;out[n]=await fn(items[n],n)}}await Promise.all(Array.from({length:Math.min(limit,Math.max(1,items.length))},worker));return out}
function classify(text){const t=norm(text);if(/rta|rebuildable tank|atomizer|atomiser|styled rta|clone rta/.test(t)){if(/mtl/.test(t)&&/top airflow|top-airflow|top air/.test(t))return{category:'RTA',typology:'MTL top airflow'};if(/mtl/.test(t))return{category:'RTA',typology:'MTL single'};if(/rdl|restricted direct/.test(t))return{category:'RTA',typology:'RDL single'};if(/dual coil|dual-coil|dual deck/.test(t))return{category:'RTA',typology:'DL dual'};if(/\bdl\b|direct lung/.test(t))return{category:'RTA',typology:'DL single'};return{category:'RTA',typology:'RDL single'}}if(/side by side|\bsbs\b/.test(t))return{category:'MODURI',typology:'side by side'};if(/squonk|bottom feeder/.test(t))return{category:'MODURI',typology:'squonk'};if(/dual battery|dual 18650|dual 21700|2x18650|2x21700/.test(t))return{category:'MODURI',typology:'dual battery'};if(/single battery|single 18650|single 21700|box mod|regulated mod|mechanical mod/.test(t))return{category:'MODURI',typology:'single battery'};if(/drip tip|mouthpiece/.test(t))return{category:'ACCESORII',typology:'drip tip'};if(/replacement glass|tank glass|pyrex/.test(t))return{category:'ACCESORII',typology:'sticla'};if(/vape cotton|wicking cotton/.test(t))return{category:'ACCESORII',typology:'bumbac'};if(/kanthal|nichrome|ni80|ss316|nife|vape wire/.test(t))return{category:'ACCESORII',typology:'sarma'};return null}
function maturity(text){const t=norm(text);if(/pre sale|presale|pre order|preorder|launching soon|release soon|waitlist open|estimated shipping/.test(t))return{stage:'IMMINENT',score:90,label:'iminent'};if(/mass production|production started|first batch/.test(t))return{stage:'PRODUCTION',score:75,label:'producție'};if(/sample sent|sample received|review sample|review coming|shipping to reviewers|first look soon/.test(t))return{stage:'SAMPLE_REVIEW',score:60,label:'mostre / review'};if(/prototype|engineering sample|pre production/.test(t))return{stage:'PROTOTYPE',score:45,label:'prototip'};if(/teaser|sneak peek|reveal soon|announcement soon|coming soon/.test(t))return{stage:'TEASER',score:30,label:'teaser'};return{stage:'RUMOR',score:15,label:'zvon'}}
function sourceType(url,cfg){const h=host(url),forums=cfg.forumDomains||[],news=cfg.independentNewsDomains||[],vendors=cfg.discoveryCommercialDomains||[],official=(cfg.officialMakers||[]).flatMap(x=>x.domains||[]);if(official.some(d=>domainMatch(h,d)))return{type:'official',eligible:true};if(forums.some(d=>domainMatch(h,d)))return{type:'forum',eligible:true};if(news.some(d=>domainMatch(h,d)))return{type:'news',eligible:true};if(domainMatch(h,'reddit.com'))return{type:'reddit',eligible:true};if(['facebook.com','instagram.com','threads.net','tiktok.com','x.com','twitter.com','youtube.com','youtu.be','t.me','telegram.me','discord.com','discord.gg'].some(d=>domainMatch(h,d)))return{type:'social',eligible:true};if(vendors.some(d=>domainMatch(h,d)))return{type:'vendor-discovery',eligible:false};return{type:'open-web',eligible:false}}
function hit(text,cfg){const t=norm(text);return (cfg.truthRules.preAnnouncementTerms||[]).some(x=>t.includes(norm(x)))||/styled rta|clone rta|1 1 clone|sxk|ulton|yftk|kindbright|shenray|coppervape|vazzling/.test(t)}
function plans(cfg){const trig='("rumor" OR rumour OR leak OR leaked OR teaser OR "sneak peek" OR "coming soon" OR prototype OR "sample received" OR "review coming" OR "production started" OR "first batch" OR preorder OR "pre-order" OR presale OR "pre-sale" OR "waitlist open" OR "release soon")',rta='(RTA OR "rebuildable tank" OR atomizer OR atomiser)',q=[];q.push({query:`${trig} ${rta}`});q.push({query:`${trig} ("RTA clone" OR "styled RTA" OR SXK OR Ulton OR YFTK OR Kindbright OR ShenRay OR Coppervape OR Vazzling)`});const fg=cfg.forumDomains||[];for(let i=0;i<fg.length;i+=4){const group=fg.slice(i,i+4).map(d=>`site:${d}`).join(' OR ');q.push({query:`(${group}) ${trig} (RTA OR atomizer OR clone)`})}const social=['facebook.com','instagram.com','threads.net','tiktok.com','x.com','youtube.com','t.me'];for(let i=0;i<social.length;i+=3){const group=social.slice(i,i+3).map(d=>`site:${d}`).join(' OR ');q.push({query:`(${group}) ${trig} (RTA OR atomizer OR SXK OR Ulton OR YFTK)`})}const vendors=cfg.discoveryCommercialDomains||[];if(vendors.length)q.push({query:`(${vendors.map(d=>`site:${d}`).join(' OR ')}) (preorder OR "pre-order" OR presale OR "pre-sale" OR "coming soon") (RTA OR SXK OR Ulton OR YFTK OR Kindbright)`});const makers=(cfg.officialMakers||[]).map(x=>x.name);for(let i=0;i<makers.length;i+=7){q.push({query:`(${makers.slice(i,i+7).map(x=>'"'+x+'"').join(' OR ')}) ${trig} ${rta}`})}return q.slice(0,24)}
function eventKey(d,cls){return hash(cls.category+'|'+cls.typology+'|'+host(d.url)+'|'+norm(d.title).slice(0,120))}
async function main(){const cfg=read(CFG,{}),prev=read(OUT,{events:[]}),prevEv=new Map((prev.events||[]).map(x=>[x.eventId,x])),search=await mapLimit(plans(cfg),6,bing),raw=search.flatMap(x=>x.docs||[]),seen=new Set(),found=[];for(const d of raw){if(!d.url||!d.publishedAt)continue;const k=d.url;if(seen.has(k))continue;seen.add(k);const ah=ageHours(d.publishedAt);if(ah==null||ah>48)continue;const text=clean((d.title||'')+' '+(d.body||''));if(!hit(text,cfg))continue;const cls=classify(text);if(!cls)continue;const src=sourceType(d.url,cfg),mat=maturity(text),id=eventKey(d,cls),old=prevEv.get(id),first=old&&old.firstSeenAt||new Date().toISOString();found.push({eventId:id,category:cls.category,typology:cls.typology,publishedAt:d.publishedAt,firstSeenAt:first,maturityStage:mat.stage,maturityLabel:mat.label,maturityScore:mat.score,sourceType:src.type,decisionEligible:src.eligible,sourceHost:host(d.url),url:d.url,title:clean(d.title).slice(0,240)})}found.sort((a,b)=>Date.parse(b.publishedAt)-Date.parse(a.publishedAt));const groups=new Map();for(const x of found){const k=x.category+'|'+x.typology+'|'+x.maturityStage;let g=groups.get(k);if(!g){g={category:x.category,typology:x.typology,maturityStage:x.maturityStage,maturityLabel:x.maturityLabel,maturityScore:x.maturityScore,items:[]};groups.set(k,g)}g.items.push(x)}const publicEvents=[...groups.values()].map(g=>{const newest=g.items.slice().sort((a,b)=>Date.parse(b.publishedAt)-Date.parse(a.publishedAt))[0],first=g.items.slice().sort((a,b)=>Date.parse(a.firstSeenAt)-Date.parse(b.firstSeenAt))[0],eligible=unique(g.items.filter(x=>x.decisionEligible).map(x=>x.sourceType)),all=unique(g.items.map(x=>x.sourceType));return{eventId:hash('heartbeat|'+g.category+'|'+g.typology+'|'+g.maturityStage),category:g.category,typology:g.typology,maturityStage:g.maturityStage,maturityLabel:g.maturityLabel,maturityScore:g.maturityScore,publishedAt:newest.publishedAt,firstSeenAt:first.firstSeenAt,sourceCount:all.length,eligibleSourceCount:eligible.length,discoveryOnly:eligible.length===0,status:eligible.length>=2?'CONFIRMED':eligible.length===1?'EARLY':'DISCOVERY'}}).sort((a,b)=>Date.parse(b.publishedAt)-Date.parse(a.publishedAt)).slice(0,20);const now=new Date().toISOString(),out={schemaVersion:1,scopeYear:2026,mode:'rapid-public-pulse',scanCadenceMinutes:15,lookbackHours:48,lastChangedAt:now,publicTypologiesOnly:true,events:publicEvents};const evid={schemaVersion:1,scopeYear:2026,mode:'rapid-pulse-evidence',lastChangedAt:now,queriesRun:search.length,queriesWorking:search.filter(x=>x.ok).length,events:found.slice(0,80)};const prevComparable={...prev,lastChangedAt:undefined},nextComparable={...out,lastChangedAt:undefined};const changed=JSON.stringify(prevComparable)!==JSON.stringify(nextComparable);if(!changed){out.lastChangedAt=prev.lastChangedAt||now;evid.lastChangedAt=read(EVID,{}).lastChangedAt||out.lastChangedAt}if(WRITE){const a=writeIfChanged(OUT,out),b=writeIfChanged(EVID,evid);console.log(`Heartbeat: ${search.filter(x=>x.ok).length}/${search.length} queries; ${found.length} raw; ${publicEvents.length} public; changed=${a||b}.`)}else console.log(JSON.stringify({queries:search.length,working:search.filter(x=>x.ok).length,raw:found.length,public:publicEvents.length,changed},null,2))}
main().catch(e=>{console.error(e&&e.stack||e);process.exitCode=1});
